// Copy to config.js locally, or let Vercel build inject from environment variables.
// The anon key is safe in the browser when RLS is enabled.
window.FVGD_CONFIG = {
  supabaseUrl: "https://ixpfcghaorhidmrtnaez.supabase.co",
  supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY",
  supabaseEnabled: false
};
